## Project Workflow Mandates

### Deployment
- **Production Deployment Command**: Always use `pnpm exec wrangler deploy --env production` to deploy to the production environment.

### Source Control (Git)
- **Commit Policy**: DO NOT commit or stage changes to GitHub/Git by default. Only perform a commit when specifically and explicitly instructed with the word "commit".
